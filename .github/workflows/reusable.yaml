on:
  workflow_call:

jobs:
  pre-commit:
    name: pre-commit
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

  npm-and-sonarqube:
    name: npm-and-sonarqube-test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
